<Application x:Class="TMC.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://metro.mahapps.com/winfx/xaml/controls"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- MahApps.Metro resource dictionaries. Make sure that all file names are Case Sensitive! -->
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" /> 
                <!-- Theme setting -->
                
                <!--<ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Light.Blue.xaml" />-->
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Label" x:Key="WindowTitle">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="Margin" Value="25 20 20 10"/>
            </Style>

            <Style TargetType="Button" x:Key="MainBtn" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Margin" Value="30 5"/>
                <Setter Property="ui:ControlsHelper.CornerRadius" Value="5"/>
                <Setter Property="Background" Value="#2747BB"/>
                <Setter Property="FontFamily" Value="Inter"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="BorderBrush" Value="#2747BB"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>
            
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}" x:Key="InfoTxtBox">
                <Setter Property="Background" Value="White"/>
                <Setter Property="ui:ControlsHelper.CornerRadius" Value="5"/>
                <Setter Property="BorderBrush" Value="Gray"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Margin" Value="0 5"/>
            </Style>
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}" x:Key="SearchBox">
                <Style.Setters>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="ui:ControlsHelper.CornerRadius" Value="10"/>
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="1.5"/>
                    <Setter Property="Margin" Value="20 0 10 0"/>
                    <Setter Property="MinWidth" Value="200"/>
                    <Setter Property="ui:TextBoxHelper.Watermark" Value="Поиск"/>
                </Style.Setters>
                <Style.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderThickness" Value="2"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="PasswordBox" BasedOn="{StaticResource {x:Type PasswordBox}}" x:Key="PasswordBoxStyle" >
                <Setter Property="Background" Value="White"/>
                <Setter Property="ui:ControlsHelper.CornerRadius" Value="5"/>
                <Setter Property="BorderBrush" Value="Gray"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Margin" Value="0 5"/>
            </Style>
            
            <Style TargetType="Button" x:Key="CancelBtn" BasedOn="{StaticResource {x:Type Button}}">
                <Style.Setters>
                    <Setter Property="Margin" Value="30 5"/>
                    <Setter Property="ui:ControlsHelper.CornerRadius" Value="5"/>
                    <Setter Property="Background" Value="#CFD9FF"/>
                    <Setter Property="FontFamily" Value="Inter"/>
                    <Setter Property="FontWeight" Value="Medium"/>
                    <Setter Property="BorderBrush" Value="#2747BB"/>
                    <Setter Property="BorderThickness" Value="1.5"/>
                    <Setter Property="Foreground" Value="#2747BB"/>
                </Style.Setters>
                <Style.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <EventTrigger.Actions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)" To="#889EEA" AutoReverse="False"  />
                                    <ColorAnimation Storyboard.TargetProperty="(Button.Foreground).(SolidColorBrush.Color)" To="White" AutoReverse="False"  />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger.Actions>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <EventTrigger.Actions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)" To="#CFD9FF" AutoReverse="False"  />
                                    <ColorAnimation Storyboard.TargetProperty="(Button.Foreground).(SolidColorBrush.Color)" To="#2747BB" AutoReverse="False"  />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger.Actions>
                    </EventTrigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="RadioButton">
                <Setter Property="BorderBrush" Value="#2747BB"/>
                <Setter Property="BorderThickness" Value="2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="10 0"/>

            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
