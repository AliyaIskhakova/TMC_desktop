<Window x:Class="TMC.Authorization"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TMC.ViewModel"
        x:Name="auth"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="t1">
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Regular"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="t2">
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Inter"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.DataContext>
            <local:AuthorizationViewModel/>
        </Grid.DataContext>
        <Grid.Background>
            <RadialGradientBrush>
                <GradientStop Color="#2747BB" Offset="-0.5"></GradientStop>
                <GradientStop Color="White" Offset="2.25"></GradientStop>
            </RadialGradientBrush>
        </Grid.Background>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Сервисный центр" Style="{StaticResource t1}"/>
            <TextBlock Text="ТехноМедиаСоюз" Style="{StaticResource t2}" />
            <Label Content="Логин"/>
            <TextBox x:Name="loginBox" Style="{StaticResource authTxtBox}"  />
            <Label Content="Пароль"/>
            <PasswordBox x:Name="passwordBox"/>
            <Button Margin="0 7" Style="{StaticResource mainBtn}" Command="{Binding AuthorizationCommand}" CommandParameter="{Binding ElementName=auth}">Войти</Button>
        </StackPanel>


    </Grid>
</Window>
